# tmpbin/homebrew-tap

Homebrew tap for tmpbin apps.

```bash
brew tap tmpbin/tap
brew install --cask hapticbreak
xattr -dr com.apple.quarantine /Applications/HapticBreak.app   # allow the unnotarized build once
```
